// 'use strict';

// const Promise = require('bluebird');

// const db = require('../../config/db');

// module.exports = (param) => {
//   	return Promise.using(db(), conn => {
//     const sql = "SELECT COUNT(URL) FROM Head";
//     console.log(sql);
//     return conn.queryAsync(sql, []);
//   });
// };
